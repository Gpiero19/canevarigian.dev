# GitHub API — used server-side to fetch repo data
# Get a fine-grained token at https://github.com/settings/tokens
# Scopes needed: Contents: Read, Metadata: Read
GITHUB_TOKEN=

# Your GitHub username — used to build API URLs
GITHUB_USERNAME=
